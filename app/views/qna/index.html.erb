<div class="container">
<h1>질문 게시판</h1>
<p>질문과 답변이 이루어질 게시판입니다.</p>

 <a 
  <% if user_signed_in? %>
    href = '/qna/new'
  <% else %>
    href = '/users/sign_in'
  <% end%>
  >글 작성하기</a>

    <div class="table-responsive" >
        <table class="table table-fit table-hover">
            <tr>
                <th width = "2%">#</th>
                <th width = "3%"></th>
                <th width = "70%">제목</th>
                <th width = "20%">작성자</th>
                <th width = "10%">작성 시간</th>
            </tr>
            <% @qnas.each do |x|%>
                <tr onclick="window.location='/qna/show/<%=x.id%>';">
                    <td><%=x.id%></td>
                    <td></td>
                    <td><%=x.title%></td>
                    <td><%=x.user_email%></td>
                    <td><%=x.created_at.strftime("%Y.%m.%d")%></td>
                </tr>
                <% if x.qnareplies.empty? %>
                <% else %>
                    <% x.qnareplies.each do |y| %>
                    <tr onclick="window.location='/qna/show/<%=x.id%>';">
                        <td></td>
                        <td>RE:</td>
                        <td><%=y.title%></td>
                        <td><%=y.user_email%></td>
                        <td><%=y.created_at.strftime("%Y.%m.%d")%></td>
                    </tr>
                    <%end%>
                <%end%>
            <% end %>
        </table>
    </div>
</div>