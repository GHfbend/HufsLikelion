<h1>퀴즈 수정</h1>
<p>퀴즈를 수정하는 페이지 입니다</p>

<form action="/quiz/update/<%=@quiz.id%>" method="post">
    <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
    제목: <input type="string" name="title" value=<%=@quiz.title%>><br>
    내용: <textarea name="content" rows="4" cols="50"><%=@quiz.content%></textarea><br>
          <input type="hidden" name="user_email" value="<%= current_user.email%>">
          <input type="submit" value="수정">
</form>
